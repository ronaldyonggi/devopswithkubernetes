apiVersion: apps/v1
kind: Deployment
metadata:
  namespace: project
  name: todo-app-dep
spec:
  replicas: 1
  selector:
    matchLabels:
      app: todo-app
  template:
    metadata:
      labels:
        app: todo-app
    spec:
      volumes:
      - name: image-cache
        emptyDir: {}
      containers:
      - name: todo-app
        image: yonggsnap/todo-app:1.7
        env:
        - name: PORT
          value: "8080"
        volumeMounts:
        - name: image-cache
          mountPath: /usr/src/app/files